(function(){
  const cursor=document.querySelector('.ponteiro');
  window.addEventListener('pointermove',event=>{cursor.style.left=event.clientX+'px';cursor.style.top=event.clientY+'px';cursor.classList.toggle('is-link',!!event.target.closest('a,button'))});
  const canvas=document.getElementById('brasiliumFallback');if(!canvas)return;const box=canvas.parentElement,ctx=canvas.getContext('2d');let w=0,h=0,angle=0,drag=false,lastX=0,lastY=0,mode=0;
  const resize=()=>{const d=Math.min(devicePixelRatio,2);w=box.clientWidth;h=box.clientHeight;canvas.width=w*d;canvas.height=h*d;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(d,0,0,d,0,0)};new ResizeObserver(resize).observe(box);resize();
  const stars=Array.from({length:180},()=>({x:Math.random(),y:Math.random(),r:Math.random()*1.5+.3,a:Math.random()}));
  function draw(){ctx.clearRect(0,0,w,h);ctx.fillStyle='#101816';ctx.fillRect(0,0,w,h);stars.forEach(s=>{ctx.globalAlpha=.25+s.a*.5;ctx.fillStyle='#83d8ba';ctx.fillRect(s.x*w,s.y*h,s.r,s.r)});ctx.globalAlpha=1;const cx=w/2,cy=h/2,scale=Math.min(w,h)/5;ctx.save();ctx.translate(cx,cy);ctx.rotate(angle);for(let i=0;i<3;i++){ctx.rotate(Math.PI/3);ctx.strokeStyle=i===mode?'#b8ff36':'#4bd6a2';ctx.shadowBlur=18;ctx.shadowColor=ctx.strokeStyle;ctx.lineWidth=3;ctx.beginPath();ctx.ellipse(0,0,scale*(1.05+i*.12),scale*(.34+i*.08),0,0,Math.PI*2);ctx.stroke()}ctx.shadowBlur=0;ctx.strokeStyle='rgba(75,214,162,.35)';ctx.lineWidth=1;ctx.beginPath();ctx.arc(0,0,scale*1.45,0,Math.PI*2);ctx.stroke();ctx.fillStyle='#b8ff36';ctx.shadowBlur=25;ctx.shadowColor='#b8ff36';ctx.beginPath();ctx.arc(0,0,scale*.18,0,Math.PI*2);ctx.fill();ctx.restore();angle+=drag?0:.006;requestAnimationFrame(draw)}draw();
  box.addEventListener('pointerdown',e=>{drag=true;lastX=e.clientX;lastY=e.clientY;box.setPointerCapture(e.pointerId)});box.addEventListener('pointermove',e=>{if(drag){angle+=(e.clientX-lastX)*.01;lastX=e.clientX;lastY=e.clientY}});box.addEventListener('pointerup',()=>drag=false);box.addEventListener('click',()=>{mode=(mode+1)%3;const label=document.getElementById('modoAtual');if(label)label.textContent=['LOW RAM MODE','4 RENDERERS','PROCESS PER SITE'][mode]});
})();
